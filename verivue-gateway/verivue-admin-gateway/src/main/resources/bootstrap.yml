server:
  port: 51603

spring:
  application:
    name: verivue-admin-gateway
  cloud:
    nacos:
      discovery:
        server-addr: #
      config:
        server-addr: #
        file-extension: yml
        name: verivue-admin-gateway
        refresh-enabled: true