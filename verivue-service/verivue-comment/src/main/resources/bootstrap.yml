server:
  port: 51807

spring:
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
  application:
    name: verivue-comment
  cloud:
    nacos:
      discovery:
        server-addr: #
      config:
        server-addr: #
        file-extension: yml
        name: verivue-comment
        # 可选：启用配置自动刷新
        refresh-enabled: true