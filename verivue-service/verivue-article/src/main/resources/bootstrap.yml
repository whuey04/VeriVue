server:
  port: 51802

spring:
  application:
    name: verivue-article
  cloud:
    nacos:
      discovery:
        server-addr: #
      config:
        server-addr: #
        file-extension: yml
        name: verivue-article
        # 可选：启用配置自动刷新
        refresh-enabled: true
  freemarker:
    template-loader-path: classpath:/templates
minio:
  accessKey: #
  secretKey: #
  bucket: #
  endpoint: #
  readPath: #
